# Generated by Ansible. The quarterly complete raw-archive verification
# (performance.md's Phase 5 archive-audit gap): every archived object
# re-hashed, not the daily 1% sample — the same quarantine-on-mismatch
# wiring as the daily sample and `archive-verify`. Expensive by design; a
# quarter apart is deliberate, not a default left unconsidered. See
# pipeline/archive_audit.py.
[Unit]
Description=SectorTrace quarterly full raw-archive verification
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory={{ state_dir }}
ExecStart=/usr/bin/docker compose -f docker-compose.yml exec -T app python -m pipeline archive-audit-full
