# This build provisions the box it runs on: clone the repo onto the VPS,
# run ansible-install.sh there, and it targets localhost over a local
# connection rather than SSH. There is deliberately no remote inventory here.
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
