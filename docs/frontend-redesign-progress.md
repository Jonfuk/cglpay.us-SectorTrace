# Public frontend redesign implementation progress

The full scope remains [the redesign plan](frontend-redesign-plan.md). Implementation is authorised by the user's explicit goal. Nothing in this progress record narrows that objective or marks an incomplete phase as complete.

## Current state

Notebook editing continuation: personal annotations now have a dedicated editor and a separate `annotation` field. Original saved note/reference text, URL, title and recorded timestamp remain unchanged. Opening an editor focuses its labelled text area. Saving or cancelling returns focus to the initiating control where it remains available. Only one edit is open at a time. A missing, changed or duplicate entry leaves the draft visible and does not overwrite another record. Imports validate optional annotation text, and collection exports retain it separately from the original reference.

Notebook validation passed: production generation, SPA fallbacks, focused lint, type checking, all 71 unit tests and the eight collection/smoke browser scenarios. The browser scenario verifies exact source punctuation, separate exported annotations, refresh persistence, mobile page width, focus restoration and a draft whose target was removed. The mobile editor capture was visually inspected. The original browser/type process handles expired during a credit interruption. The browser's saved last-run result confirmed a passing run. Type checking was rerun with an explicit completion marker and passed. The initially rejected unit launch was later accepted through the same approval path and completed with 71 passing tests. Budgets pass at 115.4 KiB shared public JavaScript, 45.4 KiB CSS and 160.8 KiB for the overview. Admin output was not rebuilt.

Navigation away with an unsaved draft, cross-tab editing, malformed legacy storage and broader accessibility acceptance remain open. Collection portability commit `ee991adc` and earlier checkpoints remain local while GitHub push approval is pending. The full redesign and mirror integration objective remains active.

Collection portability continuation: Saved views and Notebook now provide versioned JSON export and validated import, including existing version-one browser-storage envelopes. Import validates the entire file before changing the collection, retains existing entries, skips exact duplicate records and assigns a new local identifier to a conflicting imported record. Executable, external, control-character, backslash, admin and API links are rejected. Legacy route paths render as hash links without changing the stored reference. Unrecognised existing entries remain visible and exportable with inactive links rather than being silently discarded.

Storage write/remove failures now return their outcome to the collection pages. Imported work stays in memory and can be exported even when persistence fails. The interface states browser-only scope and the 10 MiB import limit. Notebook text preserves whitespace and original punctuation as saved content. Clearing the notebook has an explicit confirmation step. Obsolete star/pencil instructions and em-dash page titles have been replaced with the current Save view/Add note wording and house-style titles.

Validation: all 70 unit tests and 24 collection/public redesign/smoke browser scenarios passed. Type checking and production generation passed. Full lint found a control-character regex rule violation, which was corrected with an explicit character check. Focused lint passed after correction. The final production build, three collection unit cases and two browser cases passed again after tightening version validation and singular feedback. The final browser checks include export/import round trip, refresh persistence, repeated query keys, ID collisions, atomic invalid-import rejection, mobile notebook text and storage failure. The Saved views capture was visually inspected. Budgets pass at 115.3 KiB shared public JavaScript, 45.4 KiB CSS and 160.7 KiB for the overview. Admin output was not rebuilt.

Notebook editing, wider malformed-storage/import limits, cross-tab behaviour and assistive-technology acceptance remain open. The full redesign remains active. Earlier commits and this checkpoint remain local while the GitHub push approval is pending. No alternative push path or live deployment has been used.

Mirror verification continuation: 67 offline tests passed across `test_mirror_frontend.py`, `test_nuxt_assets.py` and the newly merged `test_docs_coverage.py`. The mirror's actual `verify_assets` routine also passed against disposable copies of the generated public and admin outputs for public-only, admin-only and both-app selections. The copies use the same `.output/public` directories copied by the Dockerfile. The public output includes the verified CQC changes. The existing admin output was checked but not rebuilt. No warehouse connection or live source was used by the asset-copy check.

Docker was not found on PATH or at the usual local executable path, and the existing Python environment has no Ansible package. Container build and Ansible check-mode integration remain unverified. This does not establish that the full deployment works. CQC checkpoint `4bca5ae9` and merge `68289900` are local. The merge incorporated the remote documentation commits without changing the verified frontend or mirror files. The pre-existing local `AGENTS.md` was backed up and restored byte-for-byte and is now an unstaged modification against the newly tracked remote file. Automatic approval review rejected the subsequent push for lack of trusted authorisation. Push approval remains pending and no alternative push path has been used.

CQC exports continuation: the route now offers CSV for every loaded registration, result-window JSON with exact request/response context, and selected-record JSON only for an unambiguous location identifier. Missing coordinates do not remove records from downloads. Missing registration arrays disable downloads, maps and pagination and retain an explicit unavailable state with retry. Duplicate location identifiers no longer silently select the first record. Export context distinguishes location provenance from omitted bulk-rating provenance and retains the response caveat and facet scope.

CQC export validation passed: focused route lint, type checking, production generation, SPA fallbacks, bundle gates and all 25 CQC export, existing public redesign and route smoke browser scenarios. New checks cover exact CSV/JSON scope, retained null provenance, comma-containing activity names, selected exports, missing arrays, duplicate identifiers and a delayed page response that must not expose exports from the preceding page. The desktop export/inspector capture was visually inspected. Public budgets remain 114.7 KiB shared JavaScript, 45.4 KiB CSS and 160.2 KiB for the overview. Admin output was not rebuilt. An initial build was rejected by automatic approval review for exhausted workspace credits. A later retry through the same approval path was accepted and completed, allowing verification to resume.

Shared inspector checkpoint `ff8e6686` is pushed to beta. CQC exports are the next checkpoint. Wider cluster presentation, mobile export, zoom and assistive-technology acceptance remain open. The full redesign and mirror integration objective remains active. No live deployment has been performed.

Shared inspector continuation: inspectors now measure their containing workspace before choosing a docked panel or modal sheet. Docked inspectors start at 360 px and resize between 320 and 560 px while retaining at least 480 px for the evidence pane plus the 24 px gap. This also supplies the missing adjacent layout for comparison, co-mention and path records. An accessible vertical separator supports arrow keys, Home/End and pointer dragging. Reset restores the preferred 360 px width. Preferences are versioned per route in browser storage, validated on read and constrained again when available space changes. Blocked storage leaves the current layout usable and displays a persistence failure message.

Shared inspector validation passed: all 57 offline Chromium scenarios across the public app, all 67 unit tests, full frontend lint, type checking, production generation, SPA fallbacks and bundle gates. New browser checks cover heading focus, keyboard/pointer resizing, exact width bounds, the evidence-pane minimum, refresh persistence, reset, container-driven modal fallback and blocked storage. A subsequent focused run also passed close-focus restoration and settled screenshot capture. The initial new test incorrectly assumed that a 1200 px viewport alone forced an overlay. Its corrected fixture constrains the containing workspace to 800 px. The final desktop capture was visually inspected after removing a smooth-scroll capture artefact. Public budgets are 114.7 KiB shared JavaScript, 45.4 KiB CSS and 160.2 KiB for the overview. Admin output was not rebuilt.

Treatment comparison checkpoint `16903ce7` is pushed to beta. Shared inspector sizing is the next checkpoint. Full manual keyboard, zoom and assistive-technology acceptance, cross-route focus restoration, local data portability and the remaining redesign and mirror integration work stay open. No live deployment has been performed.

Treatment comparison continuation: the comparison workspace now requires one unambiguous returned indicator before showing authority observations. Definition, unit, topic and substance remain visible. The response has no separate age or sex fields, so the interface explains that limitation instead of inventing demographic controls. Duplicate indicator metadata and missing units do not produce substitute charts. Missing row arrays remain distinct from empty observation sets.

Authority charts retain published confidence bounds, original periods and separate observations. Independent peer scales, groups of up to 12 observations, Chart/Data state and record inspection are explicit. England observations remain a separate reference table. Their missing bounds, source URL, retrieval date and payload hash are stated and are not filled from authority or indicator metadata. Source-reference exports preserve that distinction. Annotated chart exports include the selected indicator, source caveats and observation provenance.

Validation passed: all 67 frontend unit tests, full frontend lint, type checking, production generation, SPA fallbacks and bundle gates. The final Chromium run passed all 14 comparison and route smoke scenarios, including indicator selection, duplicate metadata, missing units, missing arrays, exact England exports, confidence-bound inspection, URL restoration and mobile inspection without page overflow. Desktop and mobile captures and an annotated PNG were inspected. The treatment chart remains a dynamic entry outside the startup import tree. Public budgets are 114.6 KiB shared JavaScript, 45.4 KiB CSS and 160.0 KiB for the overview. Admin output was not rebuilt.

Comparison charts checkpoint `8010246f` is pushed to beta. Treatment comparison is the next checkpoint. Wider comparison layout, light-theme, keyboard, zoom, failure and race acceptance remain open, along with the full redesign and mirror integration verification. No live deployment has been performed.

Compare charts continuation: charity income/expenditure, authority grant/budget and authority/provider notice-count sources now have lazy ECharts peer panels and a Chart/Data switch. Each source row retains its own dated slot, including repeated periods. Numeric nulls and non-numeric source markers remain missing rather than becoming zero. Notice charts read `count` and never plot contract values. Each peer has an explicitly independent scale including zero, with whole-number notice ticks. Charts display bounded groups of 12 records per peer while the complete returned data remains available through the existing paged table and source-reference export.

Chart bars and keyboard-accessible record buttons open the same comparison inspector as the data table. Selected records have a visible outline and accessible pressed state. Annotated SVG/PNG exports carry the displayed records, original source metadata, selected source caveats, unit, independent-scale explanation and chart-group scope. Income and expenditure use labelled series with patterns, and legend toggling is disabled to avoid an export silently omitting a measure. Chart failure leaves the record table available.

Chart validation: all 67 frontend unit tests passed, including tests for exact charity fields, repeated periods, missing values and exclusion of contract values from notice charts. Type checking, frontend lint, production generation, SPA fallbacks and bundle gates passed. All six comparison browser scenarios passed after correcting a test assertion for SVG text split across wrapped lines. The chart scenario covers exports, keyboard inspection, selection state, missing peers, the next chart group, Chart/Data switching, no refetch on local actions, notice-count fields and light theme. Exported PNG and light-theme page captures were visually inspected. Visual review prompted whole-number notice ticks. The corrected chart scenario and all five route smoke scenarios then passed together. The generated chart is a dynamic entry outside the static startup tree. Public budgets remain 114.6 KiB shared JavaScript, 45.3 KiB CSS and 159.9 KiB for the overview. Admin output was not rebuilt.

Comparison records checkpoint `8fc0e692` is pushed to beta. Charts are the next checkpoint. Treatment comparison still needs explicit indicator/unit/population selection and its own interval-aware charts. Grant/budget browser fixtures, broader multi-peer visual tests, negative-value fixtures, mobile chart layouts, keyboard/zoom coverage and final cross-route acceptance remain open. The full redesign and mirror integration verification remain active. No live deployment has been performed.

Compare records continuation: comparison tables now have 25-row local pages, URL-addressed record inspection and notebook references. Both ordinary series and individual provider pay tables use the same record component. The selected row snapshot is checked on restoration, so a missing or changed record does not acquire a replacement. Each table exports its displayed page as CSV/JSON and all of its returned rows as a source-reference JSON with selected peers, source metadata, caveats and explicit scope. Aggregate source context stays separate from row provenance, and missing row retrieval dates remain null in exports.

Comparison requests now depend on the selected entity type, keys and endpoint family rather than every URL change. Paging, inspection and switching between sources already returned by the same endpoint do not refetch the data. A response key prevents obsolete selections from being displayed. The inherited hero and its misleading layer counters have been replaced with the shared page-header treatment.

Compare records validation: type checking, full frontend lint, production generation, SPA fallbacks and bundle gates passed. The final Chromium run passed five comparison scenarios and all five route smoke scenarios. New checks cover 26-row paging, complete source-reference export, preserved null provenance, no refetch on local interaction, changed saved records and mobile inspection. An initial refresh assertion raced the URL update. The corrected test waits for the saved selection and visible inspector before refreshing, then verifies the missing-record state. Desktop and mobile captures were visually inspected. Public budgets are 114.6 KiB shared JavaScript, 45.3 KiB CSS and 159.9 KiB for the overview. Admin output was not rebuilt.

Comparison foundation checkpoint `e5c5a0e7` is pushed to beta. The record workflow is the next checkpoint. Source-specific charts and small multiples, chart/data switching, annotated chart exports, treatment slice selection, richer peer layouts and wider accessibility/failure/race checks remain outstanding. These changes do not finish Compare or any broader plan phase. The full redesign and mirror integration verification remain active. No live deployment has been performed.

Compare foundation continuation: mixed authority/provider selections now wait for an explicit type choice while retaining both saved groups. More than four active peers and duplicate identifiers produce a correction instead of truncation. A one-peer starter can load `/compare` sources. Provider pay sources wait for two peers because that endpoint requires 2–4. Requests include only the selected entity type and its exact identifiers. Retained year bounds are explicitly inapplicable. Directory loading and failure are separate from source loading, and source requests pass cancellation signals.

The route now displays one selected source at a time. Provider pay requests are made only for their selected source family, with every returned record retained rather than the former eight-row slice. Living Wage checks distinguish a missing result from a negative name check and no longer call an unsuccessful match proof of non-accreditation. Original provider pay records, caveats and source fields are available. Charity comparison retains `total_income`, `total_expenditure`, `financial_year_end` and provenance instead of mapping them to a nonexistent generic amount. Other source rows likewise retain their original fields. Peers without observations stay visible. Collection provenance is labelled as aggregate context rather than per-row evidence.

Compare foundation validation: type checking, lint, production generation, SPA fallbacks and bundle budgets passed. Three comparison scenarios and all five route smoke scenarios passed together. After the final year-bound explanation and labelled table scrolling were added, all three comparison scenarios passed again. Checks cover mixed saved selections, exact request scope, source separation, charity income/expenditure including zero, retained accounting dates, missing peers, oversized selections, all ten returned adverts and one-peer requests. The corrected desktop capture was visually inspected after resetting scroll position to remove a sticky-header capture artefact. Public budgets remain 114.6 KiB shared JavaScript, 45.1 KiB CSS and 159.7 KiB for the overview. Admin output was not rebuilt.

This is a Compare foundation checkpoint, not completion of section 25. The inherited hero and provider lists still need the planned layout. Source-specific chart adapters and small multiples, chart/data switching, complete scoped exports, URL-addressed row inspection, bounded local tables, richer treatment slice selection, mobile/light/keyboard coverage and broader failure/race checks remain outstanding. Mentioned together checkpoint `4d07e665` is pushed to beta. The full redesign and mirror integration work remain active. No live deployment has been performed.

Mentioned together continuation: `/cooccurrence` now accepts 2–5 distinct provider or supplier keys through a submitted form, with provider directory assistance and exact supplier-key entry. Repeated URL keys are preserved. Duplicate, blank and oversized selections are explained rather than silently shortened. Local record-type filtering and 25-record pages do not alter or repeat the source request. Missing arrays remain distinct from empty results.

The interface distinguishes document matching across every selected key from structured records matching two or more selected keys. This corrects the API's overly broad general note without rewriting the returned note, which remains available in metadata and exports alongside the correction. Returned matched fields remain literal, including keys or mention-type values where names were not supplied. Document excerpts retain exact source text and disclose the 400-character limit. The 200-record cap, absence of total/truncation metadata and document-first collection order are explicit.

Record inspection uses type, record identifier and document-element identifier where supplied. Duplicate identities and changed saved record snapshots remain unavailable rather than selecting a replacement. Exact document-element links are built from explicit returned identifiers. Other record types open clearly labelled broader supporting explorers. Missing source URLs, retrieval dates and payload hashes are stated, and record dates are never borrowed as retrieval dates. Displayed CSV/JSON, complete returned reference JSON and notebook references preserve matching scope and caveats.

Mentioned together checks: all 65 frontend unit tests, type checking, lint, production generation, SPA fallbacks and bundle gates passed. The first browser run passed three co-mention scenarios and five public route smoke scenarios. Tests cover submitted repeated-key scope, exact source-text exports, per-record matches, missing variants/provenance, safe document destinations, local paging, independent inspection, duplicate records and mobile retry. Light-theme and mobile inspector captures were inspected. Visual review caught the Connections active class lacking a corresponding style. The shared navigation rule now styles active section routes, and a rendered border-colour assertion covers the fix. All six co-mention and Verified paths browser scenarios passed together after that correction. Public measurements are 114.6 KiB shared JavaScript, 45.1 KiB shared CSS and 159.7 KiB for the overview. Admin output was not rebuilt.

Verified paths checkpoint `161d15d5` is pushed to beta. Mentioned together is the next checkpoint. Full-cap mixed-source fixtures, additional key-form edge cases, storage failure, keyboard/zoom coverage and final cross-route acceptance remain open. Compare, shared inspection, mirror integration checks and all unfinished parts of the full plan remain active. No live deployment has been performed.

Verified paths continuation: `/pathfinder` now has submitted start/end selectors for provider, authority and supplier endpoints and the agreed 1–6 hop bound. Provider and authority directories are available alongside exact identifier entry. Suppliers accept an exact public key because no supplier directory is supplied. Unsupported saved types and hop bounds remain visible without silently changing the request. Local inspection and presentation do not enter the API query. Mobile defaults to the ordered evidence list, with the diagram available explicitly.

Every returned hop has evidence inspection, original basis and provenance, an ordered D3/SVG diagram and a semantic list equivalent. Public profile links use explicit type/id fields from one unambiguous node record. Composite node strings are not parsed into guessed public identifiers. Saved inspection URLs retain the selected hop record, so changed evidence produces an unavailable selection rather than a replacement. Path JSON retains the complete returned response, request and limitations. Hop CSV, notebook references and annotated SVG/PNG exports are also available.

Inspection of `pipeline/web/pathfinder.py` established two material limitations now carried in the interface and exports: traversal is undirected but original edge direction is omitted from the response, and repeated edges are deduplicated by endpoint pair and relationship. The UI therefore treats relationship labels as recorded metadata and draws no directional arrows or weighted lines. Missing payload hashes and edge dates are explicit. Search-space failure, bounded no-path results, absent arrays and zero-hop responses retain distinct explanations. No-path results do not assert that entities are unconnected.

Verified paths checks passed: all 63 frontend unit tests, type checking, lint, production generation, SPA fallbacks and bundle budgets. The final combined Chromium run passed all three path scenarios and five route smoke scenarios. It covers submitted request scope, original source JSON, PNG export, keyboard hop inspection, actual public profile identifiers, saved-record changes, invalid hop bounds, duplicate node identities, missing path arrays, mobile data defaults and failed-request retry. Annotated PNG, light-theme page and mobile inspector captures were inspected. Visual review also found and corrected the Connections breadcrumb/sidebar grouping for pathfinder and co-occurrence. Browser checks verify the corrected group. The generated path diagram is a dynamic entry outside the startup import tree. Public budgets are 114.5 KiB shared JavaScript, 44.9 KiB CSS and 159.4 KiB for the overview. Admin output was not rebuilt.

Commissioning checkpoint `f63a93f6` is pushed to beta. Verified paths is the next checkpoint. Full six-hop and zero-hop browser fixtures, wider keyboard/zoom coverage, local storage failures and cross-route acceptance remain open. Mentioned together, Compare, shared inspection, mirror integration verification and the rest of the full redesign plan remain active. No live deployment has been performed.

Commissioning continuation: `/relationships` now has a lazy D3 diagram, a paged data equivalent and URL-addressed graph node and relationship inspection. One authority or provider centres the request. Retained year bounds are explicitly inapplicable. Missing arrays, missing node identities, conflicting nodes and ambiguous relationship identifiers remain visible without guessing replacements. Dated edges remain separate, with 12-edge diagram groups and 25-row data pages. Profile links and recentring use public identifiers supplied by relationship detail, never graph identifiers.

Supporting history retains the authority/provider pair scope, repeated notice matches, the response truncation flag and separate validity, publication and retrieval dates. Missing notices leave edge provenance visible. Notice values retain their original currency and are not added together. Constructed notice links are labelled. Payload hashes and per-edge derivation types are not returned, and their absence is stated in the interface and exports. CSV, JSON, returned neighbourhood manifests, pair history, notebook references and annotated SVG/PNG exports preserve their stated scope.

Diagram exports now label individual edges with display-reference numbers and include readable relationship references. Only identical source URL, retrieval date and source-system tuples share an annotation reference. Each relationship keeps its own identifier, dates and confidence. Selected edges have a dashed line and an accessible pressed state. D3 supplies path geometry while Vue owns the SVG elements. The generated manifest places the diagram outside the static startup import tree, and its lazy chunk contains the link geometry.

Commissioning validation: all 61 frontend unit tests, type checking, lint, production generation and SPA fallbacks passed. All three commissioning Chromium scenarios and five route smoke scenarios passed together after the export refinement. Checks cover exact request scope, keyboard edge inspection, source-preserving exports, actual public profile identifiers, missing notices, truncated history, mobile data defaults, detail retry and ambiguous identities. Annotated PNG, light-theme page and mobile inspector captures were visually inspected. Public bundle gates pass at 114.4 KiB shared JavaScript, 44.5 KiB shared CSS and 159.0 KiB for the overview. Existing admin output was not rebuilt, so its budget result is not fresh admin acceptance evidence.

Statements checkpoint `705a9d9e` is pushed to beta. Commissioning is the next checkpoint. Verified paths and Mentioned together currently gain only shared Connections navigation. Their full redesigns, Compare, broader graph fixtures, inspector persistence, keyboard/zoom checks and the rest of the plan remain open. Mirror configuration is committed, with container and Ansible integration verification still outstanding. No live deployment has been performed.

Statements continuation: `/claims` now presents campaign-authored evidence-backed statements. Creator, creation date, publication approver and publication date remain separate supplied fields. Missing publication metadata never falls back to creator metadata. Statement text, notes and caveats are preserved as text nodes. Original punctuation in these source fields remains unchanged. Missing citation arrays, empty citation arrays, unresolved citations and absent resolution metadata are distinct visible states.

Statement and citation inspection use URL-addressed identifiers. Duplicate statement identifiers and duplicate citation table/key pairs remain ambiguous. The selected statement is resolved against the full returned collection, so local search or 20-statement paging does not silently replace it. Search applies only after submission. Citation lists have 25-entry local pages. All local state is excluded from the claims request, and retained provider/authority/year filters are labelled as inapplicable. Citation provenance uses the supplied source URL and retrieval date, with the missing payload hash stated. No publication date is borrowed as a retrieval date.

Displayed CSV/JSON exports retain original statement caveats and citation objects, including unresolved references. The paired JSON carries the page caveat and export scope. A selected-statement JSON bundle, notebook reference and copy-link action are available in the inspector. No claim creation, automatic verification badge or generated statement is introduced.

Statements checks passed: source type checking, frontend lint, generation, SPA fallbacks and the public bundle gates. The latest public measurements are 113.9 KiB shared JavaScript, 43.9 KiB shared CSS and 157.8 KiB for the overview. Admin assets have not been freshly rebuilt since the remote integration. Three Statements browser scenarios plus all five route smoke scenarios passed. Visual inspection found an undefined control-border token in Statements and Safety. Both routes now use the defined control colour and a minimum 44-pixel control height. All three Statements scenarios and the affected Safety scenario then passed on the rebuilt source, including rendered border checks, light theme, failed-request retry, missing arrays, duplicate identifiers, independent local selection, exact source-text export and mobile inspection. Desktop dark/light and mobile captures were inspected. Broader citation paging, clipboard/storage failure, keyboard, zoom and final cross-route acceptance remain open.

Safety checkpoint `4ec2bc87` was pushed to beta. Statements is the next checkpoint. Connections, Compare, shared inspection and the rest of the full plan remain active. No live deployment has been performed.

Safety continuation: `/pfd` now has five separate source lanes and lazy PFD, SAR and HSE collection views. The source query applies only supported provider, source, relationship and year parameters. Local presentation, paging and inspection do not refetch it. Unsupported filters are explained before requesting data. The year-prefix limitation and the shared 2,000-entry cap are visible. Recognised complete dates determine chronology without changing source text. Partial date strings and undated entries have separate sections. No report ID is parsed out of a title, and a SAR library year never becomes an event date.

Each source has 25-row local pages, a data equivalent, exact relationship-aware inspection, source links, notebook references and scoped CSV/JSON/reference exports. Duplicate identities remain ambiguous. PFD sent-to and named-in entries stay separate. HSE result, original compliance date and revised compliance date remain attached to the notice. Tribunal extraction and CQC date-basis limitations are explicit. Supporting collections are labelled unfiltered and do not inherit chronology filters. Supplied collection counts, yearly summaries, areas/boards and keyword occurrences retain their distinct counting units. Missing arrays are distinguished from empty arrays. The complete held PFD report download is explicitly separate from displayed-row exports and SAR material.

Safety validation: all 58 frontend unit tests passed, then the three Safety cases passed again after a year-format correction. Type checking and lint passed. Production generation and SPA fallbacks passed. The public bundle gate passed at 113.9 KiB shared JavaScript, 43.5 KiB shared CSS and 157.5 KiB for the overview. The admin output was not rebuilt after integrating its remote changes, so the existing admin budget measurement is not new acceptance evidence. The three new Safety scenarios and five route smoke scenarios passed. After final inspector formatting, the collection and ambiguity cases passed again. A source-lane assertion was updated from raw `named_in` to the readable `Named in` label, with an additional check that the export keeps `named_in`. Desktop source lanes and the corrected mobile SAR inspector were visually inspected. These checks do not finish Safety acceptance: broader failures, large collections, cross-lane stale bookmarks, light theme, keyboard, zoom and race coverage remain open.

The frontend and mirror checkpoints were successfully pushed to beta in merge commit `51ee802e`. Safety is the next checkpoint. All plan phases remain open, including Statements, Connections, Compare, shared inspection and final acceptance. No live deployment has been performed.

Beta integration update: public checkpoint `156527ba` and mirror checkpoint `4dff7cc4` were committed. The initial push was rejected because beta had advanced. The fetched remote tip `6f3b6c2d` includes independent admin serving via `ADMIN_UI_VARIANT`, plus unrelated backend and operator changes. Integration preserves those changes without editing the admin application or backend. The public `--enable-frontend` and `--legacy-frontend` controls now coexist with that independent admin choice. The preflight validates both effective choices and only requires assets for the generated apps that are selected. Public rollback still verifies an enabled admin build. Three additional tests cover these combinations and a conflicting admin override. All 55 combined mirror and Nuxt-serving tests passed after integration, along with Python lint, Bash syntax and YAML parsing.

The treatment scenario passed again after scrolling to the top before its full-page screenshot. The corrected capture has been visually inspected and the misplaced header was a capture artefact. This check does not replace the remaining keyboard, narrow-screen, zoom and broader route acceptance work. Unwired Safety helper files remain outside these commits.

Verification update, 7 September 2026: the approval service is accepting build and test requests again. Earlier blocked or unrun entries below are historical and are superseded by this update where the same checks are named. The working-tree frontend unit suite passed all 58 tests, including three Safety helper tests that are excluded from the first checkpoint. Frontend type checking and lint passed. Production generation, SPA fallbacks and bundle gates passed, with 113.9 KiB shared public JavaScript, 42.9 KiB shared public CSS and a 156.8 KiB overview. ECharts remains lazy-loaded and the public/admin asset isolation gate passed.

The full Chromium run passed 29 of 31 scenarios. The two failing assertions were corrected to read wrapped SVG text and to await the treatment URL update before refresh. Both focused scenarios then passed. The notice PNG export was visually checked for readable source and caveat annotations. The treatment full-page screenshot contains a scrolled sticky-header capture artefact and does not prove final layout acceptance. Pay export and mobile inspection captures have also been inspected. All 26 mirror preflight and existing Nuxt-serving Python tests passed under the normal repository fixtures. No live deployment, container build, Ansible check-mode integration or full repository acceptance run has been performed.

The first beta checkpoint includes the public frontend source, fixtures, browser checks and plan. Mirror deployment changes and the unwired Safety helpers are separate pending work. All phases and the remaining acceptance work listed below remain open. Regular checkpoint commits and pushes to beta are explicitly authorised.

P0 foundations, P1 discovery, P2 profiles and geography, and P3 documents, procurement, Pay and Treatment are in progress. Every phase remains open. The user expanded the goal to include `ansible-mirror` configuration for the new frontend. Its tracked serving default and deployment checks have now changed, but no live deployment or runtime flag change has been performed. No admin or backend application code was edited.

Implemented so far:

- Public semantic dark/light tokens, local Manrope/Space Grotesk styling, grouped sidebar and a lazy Nuxt UI navigation drawer.
- Dark default with light/system choices, preserving the legacy `sectortrace-theme` preference.
- Search/type chooser, a new homepage with a neutral locator and source-category entry points, About and verification-tool index routes.
- Compact provider and authority directories with name/identifier search and query-addressed inspectors. Desktop inspection is adjacent, mobile inspection uses a full-height dialog.
- Browser collection refs are shared per app. Save actions distinguish successful persistence from a storage failure, without changing version-one envelopes.
- API parameter whitelists separate public endpoint filters from local URL state.
- Map feature-state source-layer identifiers, stale-value removal, finite-value handling, deduplicated display bands, location selection events and a list/directory fallback when mapping fails.
- A provenance disclosure showing available source dates and the full supplied hash, with missing metadata explicitly labelled. Integration into all evidence routes remains outstanding.
- A working public ESLint configuration and corrections to the findings it exposed. Browser test output now goes to a temporary directory.
- Verified legacy URL adapters for entity filters, coverage history, document passages, document tables, Places and procurement processes. Conflicting aliases produce a visible explanation. Source-supplied research links are validated before navigation.
- Coverage guide, source-period history and freshness views. History preserves the source's fiscal period labels and working evidence links. Lens navigation identifies the selected query view.
- Dedicated document passage routes with bounded context, source details, exact selection, copying and notebook references. Stale passage IDs are retained and explained without an automatic replacement.
- Document search with adjacent passage reading, keyboard/pointer resizing, persisted desktop proportions and mobile pane switching. Search failures and empty matches no longer hide the independently loaded reader.
- Seven bookmarkable provider views with separate holdings, notice-stage contract records, CQC rating origin, identity lineage and source-specific finance/legal sections. Unsearched annual reports remain visible. No combined safety/legal count is computed. A known API accounts-summary fallback to zero is excluded from the history display in favour of the original finance rows.
- Commissioning connections load on the provider Connections view and retain their exact awarded-to meaning, validity dates and available provenance. Graph entity identifiers are not converted into provider keys.
- Seven authority views now put the selected boundary before commissioning context on Overview. Funding keeps grants, budgets and lazily loaded payments separate. Contract scope states the profile's 200-notice cap and returned-versus-total count. History links separately to held source periods and procurement dates.
- Authority budget year and local Fingertips measure selection live in the URL. Lens changes preserve those selections. Absent coverage cells stay missing instead of becoming zero. Context tables retain published suppression text, source-specific caveats and an explicit statement that the rough-sleeping estimation method is not supplied by this response.
- Optional table captions, labelled scrolling regions, column scopes and per-row provenance disclosures are integrated into the authority evidence tables and profile payments. Budget responses explicitly state their missing source metadata rather than borrowing it from grants.
- A genuine synthetic PMTiles/MVT fixture now verifies boundary decoding, rendering and exact-ID focus in Chromium. The PMTiles reader no longer offsets into shared tile payload runs, with an offline regression test covering all four reused zoom-one tile IDs.
- Places now reads the closed eight-layer registry, starts on the neutral locator and offers an explicit layer preview before activation. Geography layers use their returned source periods. CQC points and coverage use their actual `/layers` response keys. Local list search, map/data choice and inspection are excluded from evidence requests.
- The Places authority list and map preserve exact ONS identity. Repeated observations remain individually readable and receive no guessed fill value. Missing values, zero, no returned observation and ambiguous observations have distinct map states. CQC points use only finite, in-range returned coordinates and open their own registration inspector.
- The homepage locator now opens an adjacent authority inspector instead of navigating directly. Authority inspection lazily loads the selected identity and separate holdings, with profile, coverage, compare and save actions.
- CQC registrations now defaults to a compact list with all six supported filters and an explicit Map/Data switch. Matching, loaded, mapped and all-page missing-coordinate counts retain their own scope. Selection links preserve filters, limit, offset and location ID, with explicit failed, empty and stale result-window states. The inspector retains registration, inspection and rating dates, API/bulk rating origins, available source details and exact entity links. It can save a registration reference or copy the scoped selection link.
- CQC requests depend only on supported filters and pagination, carry cancellation signals and clear superseded results. Rapid filter changes are queued so they cannot overwrite a preceding URL update. Desktop inspector focus now waits for the correct responsive branch, avoiding a briefly mounted mobile focus trap that could steal focus back.

## Evidence gathered

- Original baseline: 18 frontend unit tests passed. Existing bundle gate passed at 119.0 KiB shared JavaScript.
- Updated unit suite: 40 tests passed, including storage failures, preserved envelopes, API query boundaries, repeated map classification thresholds, legacy URL compatibility, shared-payload PMTiles runs, coordinate validation and ambiguous authority observations.
- Production generation and SPA fallback generation passed. The latest checked build uses 112.5 KiB shared JavaScript against the unchanged 120 KiB limit, 40.1 KiB shared CSS against 50 KiB, and 152.6 KiB for the overview against 375 KiB. Admin bundle measurements are unchanged.
- Sixteen Chromium tests passed after the provider/document work. Coverage includes legacy history and passage links, exact text safety, stale passage refusal, independent reader/search failure, keyboard resizing, mobile pane switching, tablet navigation names and provider lens scoping. Existing regression checks cover shell boot, every public list route, map failure handling and a procurement-process deep link.
- After final visual corrections, the four affected browser scenarios passed again. The strengthened provider test also proves refresh retains History, graph entity IDs are not substituted for provider keys, missing accounts income is not shown as zero, and the profile payload is fetched once during lens changes. Its initial refresh assertion raced navigation, so the test now waits for the History URL and heading before reloading.
- Desktop dark/light and settled mobile screenshots were visually inspected, including the final provider overview, document split and authority boundary. Full-page screenshots are taken after scrolling to the top to avoid scrolled sticky-header capture artefacts. The synthetic boundary screenshot proves successful PMTiles/MVT rendering and selected geometry focus. Other browser fixtures continue to cover unavailable-map behaviour. Real multi-authority scale, metric changes and point layers still require their acceptance checks.
- Type checking and public lint with error reporting passed after the profile components and reader changes. Production compilation also passed after the final typography and breadcrumb corrections. Recheck after subsequent changes.
- The authority pass generated production assets and SPA fallbacks successfully, then passed type checking, lint and unchanged bundle limits. Shared JavaScript remains 112.5 KiB, shared CSS 40.1 KiB and the map increment is 271.8 KiB. All 16 existing browser tests passed. The new authority scenario initially assumed percent-encoded fiscal-year slashes, while the router validly preserved the slash. After accepting both URL forms, that scenario passed too, including the remaining provenance and refresh assertions. The final build therefore has passing evidence for all 17 current scenarios.
- The Places pass passed all 19 browser scenarios, type checking, lint, generation and SPA fallbacks. Its strengthened scenario checks all eight endpoint choices, previews, source periods, ambiguous observations, a rendered zero-to-missing colour change, direct CQC point inspection, absence of refetches for local state, committed Data view after refresh, and a full-height mobile inspector. A separate scenario clicks the actual homepage boundary and confirms the exact authority opens without navigation. The affected map/profile scenarios passed again after final visual refinements.
- Final Places bundle gate: shared JavaScript 112.6 KiB, shared CSS 40.1 KiB, overview 152.7 KiB and map increment 272.8 KiB, all within the unchanged limits. Final desktop and mobile Places screenshots are in the temporary directory `sectortrace-redesign-places-verified`. Mobile controls fit long registry labels and show the active view visibly. All build and browser processes exited.
- The CQC pass passed all 22 Chromium scenarios, all 40 frontend unit tests, type checking, lint, production generation and SPA fallbacks. New checks exercise every supported filter, comma-bearing activity names, provider-scoped facet counts, offset reset, exact refresh reconstruction, stale selection without a substitute, failure/retry, missing and invalid coordinates, an actual offline map-point click, notebook saving, focus restoration and the full-height mobile inspector. Local selection and Map/Data changes make no extra CQC request. The tests exposed and verified corrections for rapid URL update races and desktop inspector focus timing.
- Final CQC bundle gate: shared JavaScript 112.6 KiB, shared CSS 40.6 KiB, overview 153.3 KiB and lazy map increment 269.1 KiB. All unchanged limits and public/admin isolation checks pass. Final desktop and mobile screenshots were visually inspected under the temporary directory `sectortrace-redesign-cqc-final`. Filter controls have 44 px targets and source fields wrap within the compact inspector. All build and browser processes exited.

## Useful continuation details

All changes are uncommitted in the shared checkout. Original untracked `.worktrees/`, `AGENTS.md`, `frontend/admin/test-results/` and `vue-plan.md` are unrelated and must remain untouched. New planning/progress files are owned by this task.

The available Node executable is on PATH, but npm is not. Equivalent commands in `frontend/public` are `node node_modules/@nuxt/cli/bin/nuxi.mjs typecheck`, `node node_modules/eslint/bin/eslint.js . --quiet`, `node node_modules/vitest/vitest.mjs run`, and `node node_modules/@nuxt/cli/bin/nuxi.mjs generate`. Bundling/Vitest needed normal filesystem access because sandboxed esbuild could not traverse a parent directory. Earlier runs were approved, but the latest production rebuild was rejected because the automatic approval service reported an exhausted usage limit. No alternate build path has been attempted. Type checking and lint remain available. The current generated assets do not validate subsequent source changes.

For browser tests set `PLAYWRIGHT_CHROMIUM_PATH` to the installed Chrome executable and `SECTORTRACE_E2E_OUTPUT` to a temporary output directory, then run `node node_modules/@playwright/test/cli.js test --project=chromium --workers=1`. Latest reviewed screenshots are under the temporary directories `sectortrace-redesign-provider-final` (document split) and `sectortrace-redesign-provider-verified` (provider overview). One sandboxed run stalled in local server teardown after its browser worker exited. Its exact server process was inspected and stopped, and the runner then reported all 16 tests passed. Subsequent runs with normal process access exited cleanly. No test server or build session remains active at this checkpoint.

The compatibility middleware and fixtures now cover these inspected forms: Documents uses `doc` plus `el` for reading (`documents.js:108`), Diary uses `provider` and `buyer`, document tables use `doc` and `table`, Places uses `layer` or `metric` plus `selected`, and Contracts uses `ocid` to select the lifecycle route. Legacy timeline/discrepancy entity keys are `provider` and `authority`. Compare already uses repeated `ons_code` and `provider_key`. Places still needs to consume the canonical `inspect` selection. Do not implement speculative aliases from names alone.

## Required work still open

1. Finish P0: full compatibility acceptance, supported-context explanations, cancellation across older routes, evidence table/provenance/export primitives, inspector focus/layout persistence and safe collection import/export.
2. Finish P1: complete directory caching and search-state behaviour, remaining homepage inspector/focus acceptance, broader map fixtures and all-route compatibility.
3. P2: finish provider/authority source details and lens acceptance, remaining Places map/list/inspector acceptance, CQC scope-labelled exports and remaining shared inspector/cluster accessibility acceptance.
4. P3: finish acceptance of notices/payments/process routes, question-led Pay and measure-first Treatment with uncertainty, then source-specific safety/legal lanes, published statements, remaining document search/filter/reader acceptance and document-table/revision retention.
5. P4: structured commissioning diagram, verified paths and co-mentions, same-source comparison, contextual verification, local research portability, accurate data/reference and annotated-chart exports.
6. P5: all route-specific acceptance checks, British English/editorial review, keyboard/screen-reader/zoom/reduced-motion checks, measured performance, generated-app Python serving/CSP/offline verification, full required offline regression suite and final scope audit.
7. Added deployment scope: finish `ansible-mirror` enable/rollback and preflight acceptance, including native configuration loading, Ansible execution in a suitable test environment, built-image checks and the public browser gates before release. A live deployment has not been requested or performed.

The contracts/payments/process route family is now implemented in source and is the immediate verification focus. Notices, Patterns and Payments have separate views. The old headline value totals, concentration panels and largest-provider material have been removed. Payment data loads only in its own view. Supported filters, cancellation and result signatures keep presentation state out of evidence requests. Notice rows retain every supplied supplier row under the selected notice ID, full hashes, link construction basis, process/revision/diary pivots and source-view return links. The process view preserves source-defined stages, cancellations, amendments, publication chronology and a separate undated section. Missing list-row provider identifiers/notice types and missing process hashes/link-basis metadata are stated explicitly.

Payments retain published amount text, including unparseable corrections, and keep authority-only file coverage separate from provider-filtered payment lines. Local search and ordering cover the returned response. JSON downloads state the visible/returned scope. Complete notice CSV uses the inspected existing streaming export endpoint, with pagination excluded. Payment references now use the full authority/file/row identity and refuse ambiguous older file/row-only selections. Payment notebook saving and clearing an inherited paying-authority filter are also implemented.

ECharts 6.1.0 is pinned in the public package and lockfile. The lazy `StCountChart` imports only the bar, grid, tooltip, brush, toolbox, accessibility and SVG modules. Patterns show API counts by year, quarter, procedure and value band, with an exact table. The default is now Chart as section 25 requires. Year brushing maps to publication bounds. Procedure, band and quarter selection inspect aggregates without claiming unsupported notice filtering. Stable series identifiers and explicit ARIA descriptions are present. Aggregate JSON exports include only the displayed count measure, not hidden value sums. SVG and PNG export source code includes title, measure, scope, filters, source caveats and application URL. PNG rasterisation uses a data image because production CSP does not permit blob images.

Verification completed before the latest refinements: three new Chromium procurement scenarios passed for notice filters/rows/exports/process returns, payment scope/local search, and real SVG chart rendering/zero counts/unsafe source text/year brushing. All 42 frontend unit tests passed, including the count adapter and supported year bounds. Type checking and lint passed. The earlier generated build passed the unchanged budgets at 113.8 KiB shared JS, 41.8 KiB shared CSS, 155.7 KiB overview and 269.1 KiB map increment. ECharts remained confined to a lazy chunk. Desktop pattern and payment screenshots were visually inspected in the temporary directory `sectortrace-redesign-contracts`.

The production rebuild after PNG export, full payment identifiers and subsequent accessibility/default-view refinements was rejected by automatic approval review because the approval service reported an exhausted usage limit. No alternative build execution was attempted. Source type checking and lint remain available and passed after the final data-image CSP adjustment. The expanded browser fixtures and current generated build still require runtime verification. There is no live build or browser test session. The latest generated assets predate these refinements and must not be treated as their validation.

Next steps: obtain approval capacity for the normal production generation, generate SPA fallbacks, run all public browser scenarios and bundle gates, inspect the exported PNG and final desktop/light/mobile screenshots, and correct any findings. `e2e/contracts.spec.ts` now additionally tests PNG under production-compatible image CSP, payment notebook references, clearing the inherited authority and exact/ambiguous authority-file-row selections. These added assertions have not run yet. Procurement still needs the final all-route acceptance/export review, including separate reference-manifest/CSV parity, broader missing/failed response and accessibility checks. All phases remain open.

Pay source implementation now provides all nine question views through the inspected five `source_groups`. Each view uses its own array, applicable filters, original source fields, caveats and row identifiers. Provider selections do not reach national census/statutory/ASHE/Skills for Care requests. Year bounds reach only the charity view. Unsupported retained filters are explained rather than silently applied. Every statutory age band and period remains visible. Gender-gap employer IDs stay distinct, ASHE retains `time`, `value_text` and `unit_of_measure`, suppressed strings remain strings, and zero remains zero. Census page display adds one to the documented zero-based `source_page`, while export retains the raw field with a numbering note. Charity wage rows explicitly lack underlying source URL, retrieval date and hash in this response.

`StPayRecords` provides bounded local pages, source inspection, exact query-addressed selection, stale/ambiguous selection states, notebook references and displayed-row JSON/CSV/reference-manifest downloads. Paging and Chart/Data selection do not change API filters. CSV protects formula-like source text while keeping numeric negatives numeric. The manifest retains the canonical applied query, local result window, source identifiers, original date fields, actual metadata or null and caveats. Repeat-advertised-role groups remain separate reading candidates without mixed-period salary arithmetic or vacancy claims.

The lazy `StAdvertRangeChart` implements V08 with modular ECharts custom intervals/dots and SVG rendering. It plots only explicit annual periods, preserves zero, shows one supplied bound with an open diamond, and leaves missing, non-annual and reversed-bound rows undrawn with textual explanations. It does not repair reversed bounds or annualise hourly amounts. Chart marks and labelled HTML controls inspect the exact advert. Resizing, theme updates and disposal are implemented. Annotated PNG/SVG downloads include displayed scope, filters, units, published salary text, job references, dates, source URLs, caveats and the application URL. A separate manifest carries the full reference fields. The table remains available under either presentation.

Pay validation is incomplete. Source type checking and lint passed after the chart, source views, export helpers and browser fixtures were added. A lint finding in CSV control-character handling was corrected and lint then passed. New unit cases cover source-filter boundaries, missing/suppressed values, exact employer identities, annual interval eligibility and CSV formula protection. Three new Playwright scenarios cover national scope and source fields, chart/local-page/selection/export behaviour with production-compatible image CSP, mobile inspection, and failed/absent/empty/ambiguous records. These unit and browser cases have not run. No Pay production build, browser rendering, chart-image inspection or updated bundle measurement has been completed. The production-build approval blocker remains separate from ongoing source work. Pay still needs runtime correction/acceptance, broader filter races, accessibility, light/mobile/zoom review and export checks before this route can be considered finished.

CQC uses its own filtered, paginated endpoint and shares its exact result window plus location ID. Do not treat a map-layer row as a permanent location profile or invent a location-by-ID endpoint. Regulated activities containing commas remain one string, rating origins are labelled and facet scope is explicit. The CQC endpoint returns a location source URL and retrieval date, but no payload hash or bulk-rating source URL/retrieval date. The inspector states these omissions. Remaining work includes scope-labelled exports, shared inspector resizing and broader cluster/accessibility acceptance. The unused legacy `CqcMap.vue` remains in the tree and can be removed when final component consolidation is reviewed.

Places uses the authoritative `/atlas_layers` registry. Six layers use `/geography?metric=...` with `available_years` in the server response. CQC points and coverage use separate keys inside `/layers`, which returns all layers together. The CQC map layer supplies location ID, name, region, rating, coordinates and ONS code, but no provider key or row-level provenance. Its inspector labels those omissions.

Places now handles ambiguous repeated authority observations without letting the last row choose a colour. The treatment geography query can return several observations per authority without indicator identifiers, so it cannot support a guessed cohort choice. Original rows stay accessible and ambiguity is labelled. Contract geography has no selectable year and represents the API's notice-value aggregation, never a headline spending total. Remaining Places acceptance includes cluster count presentation, multi-authority scale/style reload checks, exports, and full keyboard/focus review. Point-mode map accessible naming can be improved when integrating the CQC workspace.

Authority follow-up remains: full mobile/table accessibility review, treatment measure destination behaviour as the specialist treatment route is redesigned, source units from exact indicator metadata where supplied, and remaining profile acceptance. The backend does not return rough-sleeping estimation methods or budget source metadata in this profile. Those omissions are labelled explicitly, not reconstructed. Latest authority screenshot is in the temporary directory `sectortrace-redesign-authority-verified`. The browser server and all build sessions exited cleanly.

Provider follow-up remains: complete per-record provenance throughout the tables, human labels for returned identifier schemes, explicit missing-array versus empty-array states within every lens, and lineage loading only when the selected view needs it. Finance and pay currently shows the returned dated adverts and links to the full provider-scoped pay route. The specialist pay redesign remains open. Shared empty-state prose elsewhere still needs the final punctuation/editorial pass.

The capability limits and all 48 interview decisions remain binding. In particular, no new service registry, semantic search, inferred entity joins, cross-layer arithmetic or unsupported complete-export claims are permitted.

## Treatment and mirror continuation checkpoint

Treatment now starts with `/treatment_metrics`, with definition, source, unit, substance, authority coverage and exact periods or an explicitly limited period range. Selecting a metric maps only its supplied indicator ID or the documented NDTMS catalogue key to the specialist request. No indicator is selected from the first response row. Legacy `indicator_id` and `table_ref` selections reconstruct against the catalogue. Fingertips observations load only after both a measure and authority are selected. NDTMS without an authority presents the catalogue, with global table/publication coverage labelled as such.

Local Fingertips and England observations have separate tables and charts. England rows retain their actual missing bounds, source URL, retrieval date and hash. The reference manifest distinguishes the request's authority filter from the filters that actually constrain the England array. The chart uses source-supplied period ordering and explicit catalogue periods as a display scaffold. It does not enumerate dates from a range. Duplicate or undated periods remain separate and do not choose a chronological line. Numeric zero and suppressed `value_note` text are preserved.

NDTMS numeric observations and `other_rows` remain separate. The numeric array includes contextual populations and rates as well as estimates, so the chart requires one exact published measure and age-group selection. That selection does not filter the context/suppressed array. Observation periods are not replaced by publication editions. Both numeric ordered bounds are required for a drawn interval, zero is accepted as a bound, and reversed or incomplete bounds receive explicit explanations. A true `has_interval` flag cannot manufacture absent numeric bounds. The observation API omits publication_slug, so edition labels are not used to invent a join to the publication catalogue.

`StTreatmentRecords` provides 25-row local pages, independent source-array presentation state, exact query-addressed inspection, stale/ambiguous identities, source links, notebook saving, displayed JSON/CSV and a separate reference manifest. `StTreatmentChart` uses lazy modular ECharts line/custom/SVG modules, with visible values and confidence bounds, no smoothing, source-table equivalents and annotated PNG/SVG exports. Per-observation unit metadata is used where available. NDTMS's missing separate unit field is stated rather than guessed from a measure name.

Treatment type checking and frontend lint passed after the new source and browser fixture files were added. Four new interpretation-focused unit cases and two Playwright scenarios are written but have not run. Browser cases cover choosing a measure before requests, zero and suppressed values, local/England provenance separation, local-only selection changes, NDTMS edition/period/cohort boundaries, exports and mobile inspection. Production generation, visual verification, uncertainty-mark interaction, display-range controls, broader failures/partial/stale responses, catalogue and source-filter races, keyboard/light/zoom acceptance and final export parity remain open. No new generated build was attempted while the production-build approval rejection remains unresolved.

The user explicitly added `ansible-mirror` updates to the goal. The tracked `serve_nuxt` default is now true for both mirror modes. The wizard offers the generated frontend in both modes, and `--enable-frontend` or `--legacy-frontend` supplies the current run's choice through Ansible extra vars and persists it with `lineinfile` into the existing ignored `zz-local.yml`. Conflicting flags fail before package installation. Existing explicit local settings remain respected on ordinary redeploys. On an already configured box, Ansible check mode does not persist a toggle. First setup/reconfiguration still writes the wizard configuration as before.

The existing Docker stage already builds both frontend outputs and copies only generated static files into the Python image. No Dockerfile or admin application change was needed. A new mirror preflight runs after building the app image and before restarting the stack. It checks the effective `SERVE_NUXT` against the selected value, detects conflicting `.env.merge` overrides, and verifies both app entry points, 200/404 fallbacks and same-app local JavaScript/CSS references with the actual Nuxt resolver. Rollback does not require generated assets. The README describes the enable/rollback commands and clarifies that the shared existing flag selects public and admin entry points while Python continues serving the APIs.

Mirror checks completed: Python lint, Bash syntax, YAML parsing, and five static-asset tests passed. Those tests use only temporary fixture files and cover missing entry assets/fallbacks, external URLs, extensionless references resolving to an HTML fallback, and public references crossing into admin assets. They ran with `--noconftest` because the repository-wide conftest imports native PostgreSQL dependencies that the sandbox could not load. Two configuration-loading tests remain unrun: pydantic_core also fails to load its native library under current permissions. The attempted combined run with `tests/test_nuxt_assets.py` failed during conftest import and proves no existing seam tests passed. No container build, Ansible deployment, Ansible check-mode integration, live service restart or browser acceptance was performed. No build/test process remains live at this checkpoint.

New owned deployment files are `deploy/ansible-mirror/check_frontend.py` and `tests/test_mirror_frontend.py`. Modified deployment files are the mirror wizard, tracked vars, role tasks and README. New owned treatment files are the two treatment components, `app/types/treatment.ts`, `app/lib/treatment.ts`, its unit tests and `e2e/treatment.spec.ts`, plus the replaced treatment route. All remain uncommitted with the earlier redesign work. Unrelated shared-checkout files remain untouched.
