[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.worker"
buildCommand = "uv sync --extra postgres --extra storage"

[deploy]
startCommand = "python -m pipeline run m02_tribunals --since 2021-01-01 --limit 1"
restartPolicyType = "NEVER"
